<%- include('./partials/admin_header', { activePage: 'products' }) %>

<% if (locals.errors && errors.length > 0) { %>
  <div class="alert alert-error"><% errors.forEach(msg => { %><p><%= msg %></p><% }) %></div>
<% } %>
<% if (locals.success && success.length > 0) { %>
  <div class="alert alert-success"><% success.forEach(msg => { %><p><%= msg %></p><% }) %></div>
<% } %>

<div class="admin-card" style="max-width:650px; margin:0 auto;">
  <div class="card-header">
    <h3><i class="fa-solid fa-pen-to-square"></i> Edit Product</h3>
    <a href="/admin/products" class="admin-btn btn-secondary btn-sm">← Back</a>
  </div>

  <!-- Current Images -->
  <div style="text-align:center; margin:20px 0;">
    <div>
      <img src="/uploads/<%= product.image %>" alt="<%= product.name %>"
        style="width:150px; height:150px; object-fit:cover; border-radius:10px; border:2px solid #334155;"
        onerror="this.src='/img/placeholder.jpg'">
      <p style="color:#94a3b8; font-size:13px; margin-top:8px;">Current main image</p>
    </div>
    <% if (product.additionalImages && product.additionalImages.length > 0) { %>
    <div style="margin-top: 15px; display: flex; justify-content: center; gap: 10px; flex-wrap: wrap;">
      <% product.additionalImages.forEach(img => { %>
        <img src="/uploads/<%= img %>" alt="<%= product.name %>" style="width:80px; height:80px; object-fit:cover; border-radius:6px; border:1px solid #cbd5e1;" onerror="this.style.display='none'">
      <% }) %>
    </div>
    <p style="color:#94a3b8; font-size:13px; margin-top:8px;">Current additional images</p>
    <% } %>
  </div>

  <form action="/admin/products/edit/<%= product._id %>" method="POST" enctype="multipart/form-data" class="admin-form">
    <div class="form-group">
      <label for="edit-name">Product Name</label>
      <input type="text" id="edit-name" name="name" value="<%= product.name %>" required>
    </div>

    <div class="form-group">
      <label for="edit-category">Category</label>
      <select id="edit-category" name="category" required>
        <% categories.forEach(cat => { %>
          <option value="<%= cat %>" <%= product.category === cat ? 'selected' : '' %>><%= cat %></option>
        <% }) %>
      </select>
    </div>

    <div class="form-group">
      <label for="edit-price">Price (₹)</label>
      <input type="number" id="edit-price" name="price" value="<%= product.price %>" min="0" step="0.01" required>
    </div>

    <div class="form-group">
      <label for="edit-description">Description</label>
      <textarea id="edit-description" name="description" rows="4" required><%= product.description %></textarea>
    </div>
 
    <div class="form-group">
      <label for="edit-stock">Stock Count</label>
      <input type="number" id="edit-stock" name="countInStock" value="<%= product.countInStock %>" min="0" required>
      <small style="color:#94a3b8; font-size:12px;">Total items currently available.</small>
    </div>
 
    <div class="form-group">
      <label for="edit-sizes">Sizes (comma separated)</label>
      <input type="text" id="edit-sizes" name="sizes" value="<%= product.sizes ? product.sizes.join(', ') : '' %>" placeholder="e.g. S, M, L, XL">
      <small style="color:#94a3b8; font-size:12px;">Leave empty if not applicable.</small>
    </div>
 
    <div class="form-group">
      <label for="edit-colors">Colors (comma separated)</label>
      <input type="text" id="edit-colors" name="colors" value="<%= product.colors ? product.colors.join(', ') : '' %>" placeholder="e.g. Red, Blue, Black">
      <small style="color:#94a3b8; font-size:12px;">Leave empty if not applicable.</small>
    </div>

    <div class="form-group">
      <label for="edit-image">New Main Image (Banner) (optional)</label>
      <input type="file" id="edit-image" name="image" accept="image/*">
      <small style="color:#94a3b8; font-size:12px;">Leave blank to keep current image</small>
    </div>

    <div class="form-group">
      <label for="edit-additional-images">Add Additional Images (optional)</label>
      <input type="file" id="edit-additional-images" name="additionalImages" accept="image/*" multiple>
      <small style="color:#94a3b8; font-size:12px;">Appends to existing additional images.</small>
    </div>

    <div style="margin-top:20px; display:flex; gap:12px;">
      <button type="submit" class="admin-btn btn-primary" style="flex:1;">
        <i class="fa-solid fa-floppy-disk"></i> Save Changes
      </button>
      <a href="/admin/products" class="admin-btn btn-secondary" style="flex:1; text-align:center;">
        Cancel
      </a>
    </div>
  </form>
</div>

<%- include('./partials/admin_footer') %>
